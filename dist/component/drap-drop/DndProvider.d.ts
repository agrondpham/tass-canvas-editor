import React, { ReactNode } from 'react';
interface DragDropProviderProps {
    children: ReactNode;
}
declare const DragDropProvider: React.FC<DragDropProviderProps>;
export default DragDropProvider;
